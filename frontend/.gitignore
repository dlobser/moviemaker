# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is intentionally committed so GitHub Pages has something to serve.
# Remove this exception if you switch to the build-on-push workflow in
# .github/workflows/deploy.yml, which publishes dist without committing it.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
